name: churn-ci-project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "churn_preprocessing.csv"}
    command: "python modelling_ci.py --data_path {data_path}"
